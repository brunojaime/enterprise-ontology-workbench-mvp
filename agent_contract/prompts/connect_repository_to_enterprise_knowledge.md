Analizá el repositorio {repository} para responder: {business_question}. Usá
`ontology_search`, `ontology_describe` y `ontology_get_context`; reutilizá
términos existentes, modelá solo el contexto mínimo y adjuntá evidencia. No
uses RAG como fuente canónica ni introduzcas una ontología empresarial extensa.
